// Optional: hard-code your Firebase project here so players never see a setup box.
// Leave the placeholders in place to let each player paste their own config
// (stored in their browser's localStorage) from the in-game "Server settings" panel.
//
// These keys are NOT secrets — a Firebase web apiKey only identifies the project.
// Access is controlled by database.rules.json, not by hiding this file.
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  databaseURL: "https://your-project-default-rtdb.firebaseio.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:0000000000000000000000"
};
